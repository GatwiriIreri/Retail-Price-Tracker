import { Container, Typography } from '@mui/material';

const Suppliers = () => {
  return (
    <Container maxWidth="lg">
      <Typography variant="h4" gutterBottom>Suppliers</Typography>
      <Typography>Supplier management will go here</Typography>
    </Container>
  );
};

export default Suppliers;